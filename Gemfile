source "https://rubygems.org"

gem "ruby_llm-contract", "~> 0.10.6"

group :development, :test do
  gem "rspec", "~> 3.13"

  # Optional. Powers the bonus comparison spec
  # (spec/tribunal_comparison_spec.rb) that shows the same gate via the
  # off-the-shelf catalog assertion. Comment out if you only want the
  # custom-judge lifecycle (scripts 01-10).
  gem "ruby_llm-tribunal"
end
